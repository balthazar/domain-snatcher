{
  "name": "domain-snatcher",
  "version": "0.0.1",
  "description": "",
  "scripts": {
    "start": "node -r babel-polyfill -r dotenv/config index.js",
    "setup": "pm2 deploy ecosystem.json5 production setup",
    "deploy": "pm2 deploy ecosystem.json5 production"
  },
  "dependencies": {
    "babel-polyfill": "^6.26.0",
    "dotenv": "^6.1.0",
    "node-schedule": "^1.3.0",
    "ovh": "^2.0.1"
  }
}
